@param ch.so.agi.gretl.copilot.chat.view.ChatMessageView message
<div class="${message.cssClass()}">
    <div class="message-meta">${message.header()}</div>
    <div class="message-body">${message.templateContent()}</div>
</div>
